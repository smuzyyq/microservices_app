apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: foodrush
spec:
  selector:
    app: product-service
  ports:
    - port: 8002
      targetPort: 8002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: foodrush
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      initContainers:
        - name: wait-for-product-db
          image: postgres:15
          command: ["sh", "-c", "until pg_isready -h product-db -p 5432 -U \"$POSTGRES_USER\" -d product_db; do sleep 2; done"]
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: foodrush-secret
                  key: POSTGRES_USER
      containers:
        - name: product-service
          image: foodrush-product-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: foodrush-config
            - secretRef:
                name: foodrush-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 20
            periodSeconds: 15
