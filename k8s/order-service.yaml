apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: foodrush
spec:
  selector:
    app: order-service
  ports:
    - port: 8003
      targetPort: 8003
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: foodrush
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      initContainers:
        - name: wait-for-order-db
          image: postgres:15
          command: ["sh", "-c", "until pg_isready -h order-db -p 5432 -U \"$POSTGRES_USER\" -d order_db; do sleep 2; done"]
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: foodrush-secret
                  key: POSTGRES_USER
      containers:
        - name: order-service
          image: foodrush-order-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: foodrush-config
            - secretRef:
                name: foodrush-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 20
            periodSeconds: 15
